FILE(GLOB src "*.cpp")
FILE(GLOB hdr "*.h")
ADD_LIBRARY(OpenCSG ${src} ${hdr})
TARGET_LINK_LIBRARIES(OpenCSG ${GLEW_LIBRARIES})
INSTALL(TARGETS OpenCSG DESTINATION lib)
#INSTALL(FILES ${hdr} DESTINATION include)
